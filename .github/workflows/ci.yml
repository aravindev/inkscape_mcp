name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "**.md"
      - ".github/ISSUE_TEMPLATE/**"
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Lint + Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - name: Install Inkscape 1.4.x + system headers
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends software-properties-common
          sudo add-apt-repository -y ppa:inkscape.dev/stable
          sudo apt-get update
          # Inkscape installs WITH its recommends, matching what the README tells users
          # to run. Under --no-install-recommends the trace codepath segfaults, so CI
          # was both unrepresentative of a real install and unable to cover trace_image.
          sudo apt-get install -y inkscape
          sudo apt-get install -y --no-install-recommends \
            libcairo2-dev \
            libgirepository-2.0-dev \
            libgirepository1.0-dev \
            pkg-config \
            python3-dev
          inkscape --version

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Sync project
        run: uv sync --all-extras --dev

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      # Catches advertised-vs-implemented drift: mypy flagged the operation Literal
      # mismatch and the shadowed analysis branch before either was noticed by hand.
      - name: Mypy
        run: uv run mypy src/

      - name: Pytest
        run: uv run pytest tests/ -m "not network" -q --tb=short --ignore=tests/test_inkscape_live.py
